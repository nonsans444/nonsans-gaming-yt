services:
  - type: web
    name: nonsans-gaming
    env: static
    buildCommand: npm install && npm run build
    publishPath: ./dist
    routes:
      - from: /*
        to: /index.html
        type: rewrite
